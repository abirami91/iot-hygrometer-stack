DEVICE_MAC=
INTERVAL_SECONDS=1200
PERSISTENT_NOTIFY=1
SCAN_TIMEOUT=60
CONNECT_TIMEOUT=20
MAX_BACKOFF=60
HUMIDITY_SCALE=1.00
PRINT_RAW=0

# ---- Nightly report email (optional) ----
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_TO=
SMTP_TLS=1
